package ro.atek.qsparser.value;

/**
 * A representative interface used to denote the values which can
 * be used as a dictionary key. This can be a {@code StringValue} or
 * an {@code IntValue}. If some need to rely on a unified string
 * key, it should use {@code toString}.
 */
public interface DictKey
{
}
